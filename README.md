# OS2_partI

unzip folder
in terminal: 
    in part 2 directory use "make" command 
    "cd bin"
    "java BarrierS/BarrierTest arg[0] arg[1]"
    where:
        arg[0] = barrier size
        arg[1] = number of threads